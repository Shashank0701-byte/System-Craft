apiVersion: v1
kind: Service
metadata:
  name: systemcraft-redis-service
spec:
  selector:
    app: systemcraft-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
