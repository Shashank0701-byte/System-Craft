apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: system-craft-hpa
  labels:
    app: system-craft
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: system-craft
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Pods
          value: 2
          periodSeconds: 30
    scaleDown:
      stabilizationWindowSeconds: 300
